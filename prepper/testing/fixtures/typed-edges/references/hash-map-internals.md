---
id: 01M0Z900000000000000000508
title: Hash map internals
topic:
  - hash-maps
---

The bucket array, the probe sequence, and [[hash-map-lookup-cost|why lookups are cheap]] —
written with an alias, because the sentence needed one.

This note is also *about* [[hash-maps]], and says so twice: once in `topic`, and once here
in the body. Two fields, two edge types, one target.
