# Notes

Scratch space for the dev, at the repo root and outside the vault. Nothing here is
rendered, linked, validated, or read by any skill — it is deliberately the one file with
no contract.

If a note in here turns out to be worth keeping, it belongs in the vault: a Lesson if it
teaches, a Research note if it is an investigation, a Record if it is about what was
learned and when.
