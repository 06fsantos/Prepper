public
node_modules
.quartz-cache

# Prepper: hand-authored prose, none of it code. The vault is edited in Obsidian, the
# skills under .agents/ are either vendored and pinned by hash in skills-lock.json or
# first-party prompts (`author`), and the rest are documents whose line breaks are the
# author's. Reformatting any of it is churn.
content
prepper/testing/fixtures
.agents
.scratch
CONTEXT.md
docs/adr

# Agent worktrees are checkouts of this repo, not sources in it.
.claude/worktrees

# Prepper: a vendored build artifact. `prepper/search/vendor/` holds
# @quartz-community/search 0.1.0's built client script and compiled stylesheet, kept
# byte-for-byte apart from the marked alterations, so that re-vendoring is a diff. Prettier
# would reflow the minified script into thousands of lines and the diff with it.
prepper/search/vendor
